<UserControl x:Class="WpfApp3.A.AView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WpfApp3.A"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:AViewModel />
    </UserControl.DataContext>
    <dxmvvm:Interaction.Behaviors>
        <local:AService />
    </dxmvvm:Interaction.Behaviors>
    <StackPanel>
        <Label Content="AView" />
        <Button Content="Single Button Window" Command="{Binding OpenBViewCommand}" />
        <Button Content="Dual Button Window" Command="{Binding OpenCViewCommand}" />
        <Button Content="DragDrop Window" Command="{Binding OpenDViewCommand}" />
        <Button Content="UserControl" Command="{Binding OpenEViewCommand}" />
        <Button Content="Custom Behavior" Command="{Binding OpenFViewCommand}" />
        <Label Content="{Binding LabelResult}"></Label>
    </StackPanel>
</UserControl>