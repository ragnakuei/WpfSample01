<Window x:Class="WpfSample01.H.HView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WpfSample01.H"
        xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
        xmlns:hChildView="clr-namespace:WpfSample01.H.HChildView"
        xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
        mc:Ignorable="d"
        Title="HView" Height="450" Width="800">
    <Window.DataContext>
        <local:HViewModel />
    </Window.DataContext>
    <dxmvvm:Interaction.Behaviors>
        <dx:DXMessageBoxService/>
        <dxmvvm:CurrentWindowService />
        <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding OnLoadedCommand}" />
    </dxmvvm:Interaction.Behaviors>
    <StackPanel Orientation="Vertical" Name="LayoutRoot">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="MainView: "/>
            <Button Content="Show Message" Command="{Binding ShowMessageCommand}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="ChildView: " />
            <hChildView:HChildView dxmvvm:ViewModelExtensions.ParentViewModel="{Binding DataContext, ElementName=LayoutRoot}" />
        </StackPanel>
    </StackPanel>
</Window>
